{{-- @extends('layouts.main')

@section('container')
    <h1 class="mb-5">Artikel Kategori: {{ $category }}</h1>
    @if(count($posts))
        @foreach ($posts as $post)
            <article class="mb-5 border-bottom pb-4">
                <h2>
                    <a href="/artikel/{{ $post->slug }}" class="text-decoration-none">{{ $post->title }}</a>
                </h2>
                <p>
                    Ditulis oleh {{ $post->author->name }} | {{ $post->created_at->format('d M Y') }}
                </p>
                <p>{!! $post->excerpt !!}</p>
                <a href="/artikel/{{ $post->slug }}" class="text-decoration-none">Read more..</a>
            </article>
        @endforeach
    @else
        <p>Tidak ada artikel di kategori ini.</p>
    @endif
@endsection --}}
